/* eslint-env mocha */

describe('onboard-teams', () => {

	before((done) => {
		// onboard two users (can't be bots?) or prompt for tokens
		// write a roster with one of the two users
		done()
	})

	it('created a Team from a roster', (done) => {
		// run script non-interactively w/ first user
		// check that team was created as rostered
		done()
	})

	after((done) => {
		// cleanup teams
		// cleanup users
		done()
	})

})
