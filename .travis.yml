---
branches:
  only:
    - master
    - /^greenkeeper/.*$/
cache:
  directories:
    - "node_modules"
install:
  - npm install --ignore-scripts
language: node_js
node_js:
  - "lts/*"
notifications:
  webhooks: []
sudo: false
